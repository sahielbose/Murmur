/**
 * @murmur/mcp - the OpenSwarm MCP server exposing capture / recall /
 * summarize over Murmur's REST surface. Built in Phase 20
 * (MURMUR_CONTEXT.md §11).
 */
export const MCP_PACKAGE = "@murmur/mcp" as const;
