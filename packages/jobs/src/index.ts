/**
 * @murmur/jobs — Inngest functions: the recording-processing pipeline and
 * the scheduled resurfacing jobs. Implemented on mocks in Phase 6
 * (MURMUR_CONTEXT.md §7).
 */
export const JOBS_PACKAGE = "@murmur/jobs" as const;
